// TODO: import { createApiClient } from "@sanmart/contracts";
import { PUBLIC_API_URL } from "$env/static/public";

const createApiClient = (_baseUrl: string) => ({}) as any;

export const api = createApiClient(PUBLIC_API_URL);
